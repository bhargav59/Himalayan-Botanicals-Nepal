name: Deploy to Cloudflare Workers

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --legacy-peer-deps
      - run: npx wrangler --version
      - run: npx prisma generate
      - run: touch prisma/dev.db
      - run: npx opennextjs-cloudflare build
        env:
          DATABASE_URL: "file:./prisma/dev.db"
      - run: npx wrangler whoami 2>&1
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      - run: npx wrangler whoami --account-id c8ee2d165f87ea31e6f691341ac9ef0c 2>&1
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      - run: npx wrangler deploy .open-next/worker.js --name himalayan-botanicals-nepal --account-id c8ee2d165f87ea31e6f691341ac9ef0c 2>&1
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
